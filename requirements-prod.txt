# Requirements ottimizzati per produzione Linux
# Installa con: pip install -r requirements-prod.txt

# Include requirements base
-r requirements.txt

# ==== Production WSGI Server ====
gunicorn>=21.2.0,<22.0      # WSGI server robusto per Linux
gevent>=23.7.0,<24.0        # Async worker per gunicorn

# ==== Process Management ====
supervisor>=4.2.5,<5.0      # Process manager e monitoring

# ==== Performance & Caching ====
redis>=4.6.0,<5.0           # Cache e session store
hiredis>=2.2.0,<3.0         # Redis client ottimizzato

# ==== Monitoring & Logging ====
prometheus-client>=0.17.0,<1.0  # Metriche Prometheus
structlog>=23.1.0,<24.0     # Structured logging
sentry-sdk[fastapi]>=1.32.0,<2.0  # Error tracking

# ==== Security Hardening ====
cryptography>=41.0.0,<42.0  # Crittografia robusta
pyopenssl>=23.2.0,<24.0     # SSL/TLS support
certifi>=2023.7.22          # CA certificates bundle

# ==== Database Optimizations ====
psycopg2-binary>=2.9.7,<3.0  # PostgreSQL driver (se usi PostgreSQL)
# mysqlclient>=2.2.0,<3.0    # MySQL driver (se usi MySQL)

# ==== System Integration ====
python-systemd>=235         # systemd integration
setproctitle>=1.3.2,<2.0   # Process title modification

# ==== Health Checks ====
healthcheck>=1.3.3,<2.0    # Health check endpoints
